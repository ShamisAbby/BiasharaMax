# Generated by `flutter create --platforms=windows,macos,linux .` — see
# README "First-time setup". Not committed since these are regenerated
# from pubspec.yaml + this lib/ tree on any machine that runs that command.
/windows/
/macos/
/linux/

# Drift codegen (dart run build_runner build) — regenerated from
# lib/data/local/database.dart, not hand-maintained.
**/*.g.dart

# Standard Flutter/Dart build output.
/build/
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
pubspec.lock

# IDE
.idea/
*.iml
